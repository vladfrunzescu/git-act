on: pull_request_target

jobs:
  my_job:
    name: Auto-approve
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          script: |
            console.log("start")
